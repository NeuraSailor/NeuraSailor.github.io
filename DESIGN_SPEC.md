# NeuraSailor 知识花园 V2 重构手册

## 项目当前状态

MkDocs Material 站点，`docs/` 下有 247 篇 Obsidian 笔记 + 494 张图片，覆盖大一~大二 20+ 门课程。Git 基准 commit：`339008f`（main 分支），当前可以正常 `mkdocs serve`，站点外观为 V1 的粉色主题。

内容使用 Obsidian 编写，包含大量 `[[wikilink]]` 双向链接和 `> [!note]` 格式的 callout。文件按课程文件夹组织，例如 `学习笔记/大一上/线性代数/`。

---

## 一、期望的最终效果

### 1. 视觉主题 —— 霍格沃茨魔法世界

核心感觉：走进霍格沃茨图书馆——深色木质书架、金色烫字、羊皮纸质感、暖色烛光。

参考 /hogwarts-theme

**配色：**

亮色模式下，主体应为深海军蓝基调，暗金色作为强调色，正文偏奶油/羊皮纸色。Hero 区域用深蓝渐变背景。顶部有一条 4px 的彩色渐变装饰条。

暗色模式是**暖暗色**（像烛光下的密室），不是纯黑或冷灰。仍然保持暗金强调，但背景更深，文字更暖。

具体色值上下浮动 5% 均可：

- 主背景：深海军蓝 `#0a0a2e`
- 强调色/链接/标题装饰：暗金 `#d4a855`
- 正文：奶油色 `#e8d5b0`
- 卡片背景：暖白 `#f5f0e8`
- 暗色模式背景：`#1a1a2e`，卡片 `#242438`

**字体：**

- 标题：ZCOOL XiaoWei（站酷小薇体，有手写感的中文字体）
- 正文：Noto Serif SC（思源宋体，适合长文阅读）
- 代码：JetBrains Mono
- 均从 Google Fonts 加载

**排版：**

- h1 居中，下方有暗金色短横线装饰
- h2 左侧有暗金色竖线
- h3 左侧有暗金色菱形点 ◆
- 链接统一暗金色，鼠标悬停时变亮并出现下划线
- 引用块左侧暗金边框
- 表格暗金表头，隔行有微弱背景色差异

**组件：**

- 顶部导航栏深蓝底金色字
- 页脚深蓝底奶油色字
- 页面顶部有 2px 暗金阅读进度条，随滚动增长
- 侧边栏默认收起的，由一个独立按钮控制开关（在任何页面都能用）
- Callout 提示框（Obsidian 的 `> [!note]` 等）显示为魔法书风格的边框卡片
- 亮/暗模式可手动切换

### 2. 首页

首页不再是纯文本 markdown，而是一个有视觉冲击力的**着陆页**，结构和内容如下：

1. **Hero 区**：顶部大面积深蓝渐变背景，中央大号标题"NeuraSailor 知识花园"，副标题"探索知识，记录成长，构建思维的魔法世界"，暗金色装饰分隔线
2. **三个导航卡片**：水平排列的三张卡片，分别是"魔药学"（链接到学习笔记）、"机械臂"（链接到机器人项目博客）、"唱唱反调"（链接到杂谈/博客区），每张卡片有图标、简短介绍、点击跳转的按钮
3. **学期时间线**：垂直时间线展示大一上、大一下、大二上三个学期，节点用金色圆点和暗金连线

首页需要在所有设备上正常显示（桌面端三列卡片，手机端堆叠为一列）。

### 3. 导航结构 —— MOC 风格

站点导航不是按文件名或时间排序的扁平列表，而是**分层的手绘目录**：

- 侧边栏显示完整的课程树：首页 → 学习笔记 → 学期（大一上/大一下/大二上）→ 具体课程 → 课程下的笔记页
- 每个学期有一个总览页（MOC），列出该学期所有课程以及每门课的笔记数
- 博客区独立为一个板块，有一个索引页
- Obsidian 的 `.obsidian/` 配置文件夹不出现在站点中

### 4. Obsidian 风格交互（关键功能）

**Wikilink 双向链接：**

- 笔记中的 `[[另一篇笔记]]` 在站点中渲染为可点击的链接，点击后跳转到对应页面
- 链接文本只显示笔记名，不显示完整路径

**悬停预览（类 Obsidian 的 Page Preview）：**

- 鼠标悬停在 wikilink 上时，弹出一个小浮窗，预览笔记
- 如果链接目标是文件夹而非单个笔记，则不显示预览
- 弹窗不闪烁，一直显示直到鼠标移开

**Callout 渲染：**

- Obsidian 格式的 `> [!note]`、`> [!warning]`、`> [!tip]` 等 callout 正确渲染为样式化的提示框
- callout 支持折叠（`> [!note]+` 为默认展开，`> [!note]-` 为默认折叠）
- callout 标题支持中文别名（比如 `> [!提示]` 等同于 `> [!tip]`）

**侧边栏开关：**

- 侧边栏默认隐藏，页面左上角有一个明显的按钮
- 点击按钮打开/关闭侧边栏
- 这个按钮在**所有页面**上都有效，不只是首页

**导航过程中的状态保持：**

- 所有交互元素（进度条、侧边栏按钮、悬停预览）在页面之间跳转后仍然正常工作，不需要手动刷新页面

### 5. 公式和图表

- 笔记中的 LaTeX 数学公式正确渲染（MathJax）
- Mermaid 图表正确渲染

### 6. 博客区（唱唱反调）

独立的博客板块，有索引页列出所有博文。博文按时间排列。

### 7. 搜索

支持中文、英文关键词搜索，中文分词准确。

---

## 二、实施流程 —— 分模块逐步构建

5 个模块，**串联依赖**。完成一个、验证通过、再进入下一个。每个模块只专注一个领域。

### 模块 1：主题地基

**做的事：** 把现有的 V1 粉色主题替换为霍格沃茨暗金深蓝主题。

- 更新站点配置：切换为手动亮/暗模式、指向新的自定义模板目录
- 加载新字体（ZCOOL XiaoWei、Noto Serif SC、JetBrains Mono）
- 编写亮色主题的完整样式：配色变量、排版、导航栏、侧边栏、表格、引用块、代码块、callout、搜索框、滚动条
- 编写暗色主题样式（暖暗色覆盖）
- 创建阅读进度条（页面顶部 2px 暗金线，随滚动增长）
- 删除旧的粉色主题文件
- 排除 `.obsidian/` 不出现在站点中

**完成后效果：**

- 打开站点，整体是深蓝+暗金的魔法世界风格
- 顶部有渐变色装饰条
- 页面顶部有阅读进度条，滚动时增长
- 亮/暗模式切换按钮可用
- 标题、链接、表格、引用块等元素的样式正确
- 数学公式正常渲染
- 所有 247 篇笔记可正常访问

### 模块 2：首页

**做的事：** 在模块 1 的基础上，创建一个视觉冲击力强的 HTML 着陆页，替代现有的纯文本首页。

- 删除旧的独立首页文件（如果有）
- 创建新的首页：Hero 区 + 三张导航卡片 + 学期时间线 + 统计数字 + 页脚
- 桌面端三列、手机端自适应堆叠

**完成后效果：**

- 打开站点首页，看到完整的着陆页而非纯 markdown 文本
- Hero 区域有深蓝渐变背景和金色标题
- 三张导航卡片的链接正确指向学习笔记和博客区
- 时间线和统计数字可见
- 跳转到任意笔记页后模板正常，没有布局错误

### 模块 3：导航 + MOC

**做的事：** 在模块 2 的基础上，建立分层目录导航和各学期的 MOC 索引页。

- 侧边栏显示完整的树形导航：首页 → 学习笔记 → 三个学期 → 各课程 → 笔记页
- 为大一上、大一下、大二上分别创建 MOC 总览页，列出该学期所有课程及笔记数
- 学习笔记总页作为学期枢纽
- 侧边栏默认隐藏，有一个独立按钮可以打开/关闭

**完成后效果：**

- 侧边栏导航是完整的树形结构，不是扁平列表
- 每个学期 MOC 页列出该学期所有课程
- 导航层级逻辑通顺：首页 → 学期 → 课程 → 笔记
- 侧边栏按钮在所有页面可用，点击切换侧边栏显示/隐藏

### 模块 4：链接预览

**做的事：** 在模块 3 的基础上，让 wikilink 获得 Obsidian 风格的交互体验。

- `[[笔记名]]` 在站点中渲染为可点击链接，点击跳转到对应页面
- 鼠标悬停 wikilink 时弹出浮窗，显示目标笔记的标题和正文摘要前几行
- 文件夹链接不显示预览
- 弹窗样式与霍格沃茨主题一致

**完成后效果：**

- 笔记正文中的 `[[xxx]]` 是蓝色/金色可点击链接
- 鼠标悬停在链接上约半秒后出现预览窗口
- 移开鼠标后预览窗口消失
- 在页面之间跳转后预览功能仍然正常

### 模块 5：收尾

**做的事：** 最后的整合工作。

- 唱唱反调博客区索引页
- RSS 订阅
- 内容更新辅助脚本
- README 和 .gitignore 更新
- 确保 `mkdocs build --strict` 零错误零警告

**完成后效果：**

- 博客区可访问，文章列表正确
- RSS 可用
- 构建零错误
- 可以部署到 GitHub Pages

---

## 三、工作方式

### 环境准备

进入项目目录后先安装依赖，使当前 V1 基准可以正常运行 `mkdocs serve`。V1 的 `mkdocs.yml` 中引用的插件都需要安装。

### 开发节奏

1. 从 main（`339008f`）创建第一个分支，完成模块 1
2. 做完后 `mkdocs build --clean --strict` 确认无错误，停止。
3. 由用户`mkdocs serve` 启动本地预览
4. **不要自己判断完成**——让用户打开浏览器验证。用户确认通过后才创建下一个分支，有问题用户会输入问题与你讨论解决问题。
5. 下一个分支基于上一个分支创建，串联进行
6. 全部 5 个模块通过后，依次合入 main

### 关键规则

1. **所有交互元素必须在跨页面导航后仍然正常工作**，不能只在首页或首次加载时有效。页面间的导航可能是异步的（不会触发整页刷新），所以任何绑定到页面元素的逻辑都需要考虑这一点。
2. **不要改动笔记的 Obsidian 源格式**。笔记中的 `[[wikilinks]]` 和 `> [!callout]` 保持原样，由构建工具在生成站点时转换。
3. **每次开发前**，使用项目根目录下的 `serve.ps1` 启动本地服务器（它会先终止旧的 mkdocs 进程，避免 Windows 下的多进程端口冲突问题）。严禁直接输入 `mkdocs serve`。
4. **Git commit message 用中文**。
