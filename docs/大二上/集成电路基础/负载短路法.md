1. 可以使用虚断，不能使用虚断（因为不能保证在线性放大区）
2. 对于串联负反馈，要求 $V_f=0$
3. 对于并联负反馈，不一定要求 $I_f=0$，只需要"反馈回来的信号量为 0"，即
   - 如果采样点（即反馈网络的取样端）电位被强制拉低为 0，导致没有任何信号能从输出端传回输入端。
   - 那么结论就是：反馈信号消失 → 电压反馈。

> [!example]-
> ![[overlay_1766214865061.png]]